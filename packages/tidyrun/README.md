# TidyRun

Local-first optimization runtime for coding agents. No extra model, API key,
account, or telemetry service.

```bash
npx tidyrun init
tidyrun run -- npm test
```

The published package contains a standalone bundled CLI. See the repository
README for benchmark methodology, provider limitations, and recovery commands.
