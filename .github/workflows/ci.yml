name: CI

on:
  push:
  pull_request:

jobs:
  verify:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - run: npm run lint
      - run: npm run benchmark
      - run: npm run package:smoke
      - run: npm pack --dry-run -w @tidyrun/core
      - run: npm pack --dry-run -w @tidyrun/cli
      - run: npm pack --dry-run -w tidyrun
